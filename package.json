{
"scripts": {
  "build": "rm -rf public && mkdir public && copyfiles *.html *.js *.css *.json *.txt *.xml public && copyfiles -u 1 \"libs/**/*\" \"img/**/*\" \"video/**/*\" public && cp _worker.js ./public/_worker.js"
},
  "devDependencies": {
    "copyfiles": "^2.4.1"
  }
}
